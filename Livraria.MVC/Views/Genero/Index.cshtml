@model IEnumerable<Livraria.MVC.ViewModels.GeneroViewModels>

@{
    ViewBag.Title = "Index";
    Layout="~/Views/shared/_LayoutAdministrativo.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GeneroId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GeneroId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GeneroId })
        </td>
    </tr>
}

</table>
